import { StyleSheet } from "react-native";

export const tabBarStyle = StyleSheet.create({
    container: {
        color: "red",
        backgroundColor: "black"
    },
    icon: {
        backgroundColor: "red",
        color: "yellow",
    }
});